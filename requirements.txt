# Sentinel — core dependencies (testnet phase)

# Required now: Ed25519 signing/verification in the attestation core.
cryptography>=42

# Real Postgres client for the postgres.query MCP tool. Imported lazily, so the
# mock-backed tests, CI and demos run without a database driver present.
psycopg[binary]>=3.1

# Bittensor SDK for the miner/validator neurons. v11 has no axon/dendrite/synapse
# networking stack: endpoints are published on-chain with the ServeAxon intent and
# served over our own HTTP layer, authenticated via bittensor.http_auth.
bittensor>=11.1

# Pinned during testnet build.
# fastapi>=0.110
# uvicorn>=0.29
# pydantic>=2.0
# httpx>=0.27
